<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use App\Models\User;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {


        // $users = User::all();

        // foreach($users as $user){
        //      \App\Models\Tweety::factory()->count(2)->create(['user_id'=> $user->id]);
        // }


    }
}
